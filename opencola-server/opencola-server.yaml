env: server

server:
  host: "0.0.0.0"
  port: 5795

storage:
  path: "../storage/"

security:
  keystore:
    name: "keystore.pks"
    password: "password" # This should be typed in by user and removed from here

