name: test

eventBus:
  name: "event-bus"
  maxRetries: 3

server:
  host: "0.0.0.0"
  port: 6000

storage:
  path: "../storage/test"

security:
  keystore:
    name: "keystore.pks"
    password: "password"

search:
  solr:
    baseUrl: "http://localhost:8983/solr/"
    connectionTimeoutMillis: 10000
    socketTimeoutMillis: 60000

network:
