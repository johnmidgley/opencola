services:
  oc:
    build: ..
    ports:
      - "5795:5795"
    volumes:
      # Volumes for windows machines
      #- "$APPDATA\\opencola\\storage\\:/opencola/storage"
      
      # Volumes for mac/*nix
      - ~/.opencola/storage:/opencola/storage
    #working_dir: /opencola/server
    #run: /bin/sh start
    restart: unless-stopped