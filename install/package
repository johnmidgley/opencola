#!/bin/bash

if [ $# -ne 1 ]
then
  echo "Usage: $0 archive-name"
  exit 1
fi

./package
mkdir OpenCola-"$1"
cp -r Dockerfile opencola mac windows linux OpenCola-"$1"
zip -r OpenCola-"$1".zip OpenCola-"$1"
rm -rf OpenCola-"$1"
