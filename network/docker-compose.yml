version: '3'
services:
  solr:
    image: solr:8.10
    ports:
     - "8983:8983"
    volumes:
      - ../storage/var-solr:/var/solr

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: example

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
